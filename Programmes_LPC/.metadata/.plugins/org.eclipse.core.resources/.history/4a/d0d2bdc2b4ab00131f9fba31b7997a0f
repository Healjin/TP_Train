#include "Images.h"

 typedef struct {
     const uint8_t *data;
     uint16_t width;
     uint16_t height;
 } tImage;

void Draw_Images(tImage *image)
{
	int size_array = image->height * image
	int i;
	for (i = 0; i < image->; i++) {
		Write_pixel();
	}
}
